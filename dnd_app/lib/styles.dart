import 'package:flutter/material.dart';

/// Shared stat/text styles used across the app UI.
class StatStyles {
  // Primary numeric/stat font size used for combat numbers and HP
  static const double fontSize = 18.0;

  // Baseline position used for baseline alignment
  static const double baseline = 18.0;

  // TextStyle for stat numbers
  static const TextStyle textStyle =
      TextStyle(color: Colors.white, fontSize: fontSize);
}
